# Pokecomparator

This project aims to build a pokemon comparator using the data available at https://pokeapi.co

the features to implements are the following : 

- retrieve and shows the pokemons information (name, image and types)

- display the pokemon information in a graphic chart

- allow the research by ID or name

- implement a comparison functionnality between two given component